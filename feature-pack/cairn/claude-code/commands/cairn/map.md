---
description: 복구 그래프(recovery-map) mermaid 생성
argument-hint: [--focus <node>] [--render]
allowed-tools: Bash
---

# /cairn:map

복구 그래프(recovery-map) mermaid 생성

## 실행
```bash
bash ~/.cairn/current/bin/cairn map $ARGUMENTS
```
