---
description: 복구 노드 재앵커 + baton resume 연결
argument-hint: --to <node>
allowed-tools: Bash
---

# /cairn:return

복구 노드 재앵커 + baton resume 연결

## 실행
```bash
bash ~/.cairn/current/bin/cairn return $ARGUMENTS
```
