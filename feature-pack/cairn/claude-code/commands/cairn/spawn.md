---
description: 복구 메타 분기 — 하위 태스크 생성
argument-hint: <name> --from <parent> [--worktree <ref>] [--session <ref>] [--return-to <node>]
allowed-tools: Bash
---

# /cairn:spawn

복구 메타 분기 — 하위 태스크 생성

## 실행
```bash
bash ~/.cairn/current/bin/cairn spawn $ARGUMENTS
```
