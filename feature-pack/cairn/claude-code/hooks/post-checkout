#!/bin/sh
# cairn git post-checkout hook - worktree/node mapping check.
if command -v cairn >/dev/null 2>&1; then
  cairn reconcile || true
elif [ -x "$HOME/.cairn/current/bin/cairn" ]; then
  "$HOME/.cairn/current/bin/cairn" reconcile || true
fi
