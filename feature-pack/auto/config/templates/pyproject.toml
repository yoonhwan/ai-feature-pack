[project]
name = "autoresearch"
version = "0.1.0"
description = "AutoResearch — autonomous experiment loop"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
]
# Add project-specific dependencies here
# e.g. "torch>=2.0" for ML training
